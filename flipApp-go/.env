# JWT Configuration (shared across all services)
JWT_SECRET=changeme

# Consul Configuration
CONSUL_ADDRESS=consul:8500

# Service Addresses
SERVICE_ADDRESS=

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: ALLOWED_ORIGINS=http://localhost:3000,http://your-public-domain.com
ALLOWED_ORIGINS=http://tokpro.kz,http://localhost:8081,http://localhost:8082

# Database Configuration (shared across all services)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=flipapp
DB_USER=user
DB_PASSWORD=password
DB_SSLMODE=disable
